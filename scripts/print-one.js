// Prints the number 1 to stdout.
//
//   node scripts/print-one.js
//
// A smoke-test target for the orchestration pipeline: the smallest change that still has to be
// written, run and committed end to end, so a dispatch can be verified without a real feature
// riding on it.

console.log(1)
